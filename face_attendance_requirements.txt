# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=9.0.0
faiss-cpu>=1.7.4
insightface>=0.7.3
onnxruntime>=1.17.0
scipy>=1.10.0

# Face detection options (choose at least one)
# RetinaFace (via insightface) is the default and ships with the package above
# MTCNN / MediaPipe remain available if you prefer them
mtcnn>=0.1.1
mediapipe>=0.9.0

# GUI
tk  # Usually included with Python

# Optional but recommended
matplotlib>=3.5.0  # For visualization
scikit-learn>=1.0.0  # For evaluation metrics

# Anti-spoofing (SilentFace)
torch>=2.1.0
